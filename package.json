{
  "name": "traffic-scraper",
  "version": "1.0.0",
  "description": "Scrape UAE traffic violations using Playwright",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "npx playwright install --with-deps chromium",
    "postinstall": "npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.5",
    "playwright": "^1.43.1"
  }
}
